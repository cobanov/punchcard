// Package service holds business logic and authorization decisions.
// Authorization for each operation type lives here in exactly one place;
// transport handlers never re-implement it.
package service
